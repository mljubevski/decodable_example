# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Decodable Example' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Decodable Example
  pod 'Decodable', :git => 'https://github.com/Anviking/Decodable.git', :tag => '0.5.1'
  pod 'Alamofire', '~> 4.4'


end
